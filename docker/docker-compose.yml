services:
  postgres:
    image: postgres:18-alpine
    container_name: yaci-explorer-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-yaci}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      POSTGRES_DB: ${POSTGRES_DB:-yaci}
      PGDATA: /var/lib/postgresql/18/docker
    volumes:
      - postgres-data:/var/lib/postgresql
      # Minimal init - just create api schema, let yaci run its own migrations
      - ./init-api-schema.sql:/docker-entrypoint-initdb.d/init-api-schema.sql:ro
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-yaci}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - yaci-network

  postgrest:
    image: postgrest/postgrest:v12.0.2
    container_name: yaci-postgrest
    environment:
      PGRST_DB_URI: postgres://${POSTGRES_USER:-yaci}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/${POSTGRES_DB:-yaci}
      PGRST_DB_SCHEMA: ${PGRST_DB_SCHEMA:-api}
      PGRST_DB_ANON_ROLE: ${POSTGRES_USER:-yaci}
      PGRST_SERVER_PORT: 3000
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - yaci-network

  yaci:
    image: ghcr.io/cordtus/yaci:main
    container_name: yaci-indexer
    command: >
      extract postgres
      --postgres-conn postgres://${POSTGRES_USER:-yaci}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/${POSTGRES_DB:-yaci}
      --live
      --enable-prometheus
      --start ${YACI_START_HEIGHT:-1}
      --max-concurrency ${YACI_MAX_CONCURRENCY:-5}
      -k
      ${CHAIN_GRPC_ENDPOINT}
    environment:
      YACI_GRPC_ENDPOINT: ${CHAIN_GRPC_ENDPOINT}
      YACI_POSTGRES_DSN: postgres://${POSTGRES_USER:-yaci}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/${POSTGRES_DB:-yaci}
      YACI_INSECURE: ${YACI_INSECURE:-false}
    ports:
      - "2112:2112"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - yaci-network
    extra_hosts:
      - "host.docker.internal:host-gateway"

  explorer:
    build:
      context: ..
      dockerfile: docker/explorer/Dockerfile
    container_name: yaci-explorer
    ports:
      - "3001:80"
    depends_on:
      - postgrest
    restart: unless-stopped
    networks:
      - yaci-network

networks:
  yaci-network:
    driver: bridge

volumes:
  postgres-data:
